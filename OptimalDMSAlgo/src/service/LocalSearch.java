package service;

import java.util.List;

import objects.SwimmingClub;

public class LocalSearch {

    /*
     * public static int hillClimbing(SwimmingClub club, boolean isMale) {
     * 
     * TeamState currentState = new TeamState(club, isMale); // initialize current
     * state with the given club
     * 
     * while(true){
     * List<TeamState> neighbors = generateNeighbors(currentState);
     * TeamState bestNeighbor = getBestState(neighbors);
     * if(valueOf(bestNeighbor) <= valueOf(currentState)){
     * break;
     * }
     * currentState = bestNeighbor;
     * }
     * 
     * 
     * return valueOf(currentState); // return the value of the final state
     * }
     */

}
